// Route: / → workspace screen (parity: "workspace" screen in prototype)
import WorkspaceScreen from "@/components/workspace/WorkspaceScreen";

export default function HomePage() {
  return <WorkspaceScreen />;
}
