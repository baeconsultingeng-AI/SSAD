// Route: /settings → parity: "settings" screen in prototype
import SettingsScreen from "@/components/settings/SettingsScreen";

export default function SettingsPage() {
  return <SettingsScreen />;
}
